/* GENERAL NAVBAR STYLES */
/* .custom-navbar {
} */

#homepage-navbar {
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  width: 100%;
  max-width: 1440px;
  height: 100px;
  background-color: #fff;
  box-shadow: 0 1px 4px rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
  z-index: 103;
  padding: 0 80px !important;
  border-bottom: 1px solid #0d0c120d;
}

.navbar-container {
  padding: 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 25px;
}

.navbar-nav {
  display: flex;
  justify-content: space-around;
  width: 100%;
}

.navbar-nav .nav-link {
  position: relative;
  font-weight: 500;
  color: #333;
  padding: 0 !important;
  cursor: pointer !important;
}

.nav-item {
  padding: 0 !important;
}

/* ACTIVE LINK UNDERLINE */
.logo.active-link {
  position: relative;
}

.navbar-nav .nav-item.active-link .nav-link::after {
  content: "";
  position: absolute;
  width: 100%;
  height: 2px;
  bottom: -3px;
  left: 0;
  background: linear-gradient(270deg, #ffdb58 0%, #801323 100%);
}

.logo.active-link::after {
  content: "";
  position: absolute;
  width: 100%;
  height: 2px;
  bottom: -12px;
  left: 0;
  background: linear-gradient(270deg, #ffdb58 0%, #801323 100%);
}

.dropdown-link {
  display: flex;
  align-items: center;
  gap: 5px;
  text-decoration: none;
  cursor: pointer;
}

.nav-link:hover,
.dropdown-link:hover {
  color: black;
}

/* GRADIENT BUTTON */
.gradient-button-container {
  width: fit-content;
  padding: 2px !important;
  border-radius: 10px;
  background: linear-gradient(90deg, #ffdb58 0%, #801323 100%);
}

.gradient-button {
  padding: 10px 20px;
  border: none;
  border-radius: 10px;
  background-color: white;
  font-family: "Schibsted Grotesk", sans-serif;
  cursor: pointer;
  transition: all 0.3s ease-in-out;
  color: #0D0C12;
  font-size: 16px;
  font-weight: 500;
}

.gradient-button:hover {
  background-color: white;
  color: #0D0C12;
}

/* DROPDOWN WRAPPER */
.navbar-nav .nav-item.dropdown {
  position: relative;
}

/* CUSTOM DROPDOWN STYLES */
.custom-dropdown {
  display: block;
  position: absolute;
  top: 100%;
  left: -100%;
  min-width: 220px;
  box-shadow: 4px 10px 20px 0 rgba(0, 0, 0, 0.20);
  border-radius: 5px;
  z-index: 1000;
  border: none !important;
}

.dropdown-menu {
  margin-top: 32px;
  padding: 0 !important;
}

.custom-dropdown .dropdown-item {
  font-family: "Schibsted Grotesk";
  font-size: 16px;
  font-weight: 500;
  line-height: normal;
  text-align: left;
  padding: 20px;
  white-space: nowrap;
  color: #333;
  text-decoration: none;
  display: block;
  transition: background-color 0.2s ease;
  position: relative;
}

.custom-dropdown .dropdown-item::after {
  content: "";
  position: absolute;
  top: 100%;
  left: 4%;
  height: 1px;
  width: 90%;
  background-color: rgba(88, 65, 65, 0.1);
}

.custom-dropdown .dropdown-item:hover {
  background-color: #f0f0f0;
  color: #000;
}

/* Submenu container */
.dropdown-submenu {
  position: relative;
}

/* Submenu content fly out to the right */
.dropdown-submenu .right-submenu {
  position: absolute;
  top: -50%;
  left: 99%;
  background: #fff;
  display: none;
  border: none !important;
  box-shadow: 4px 10px 20px 0 rgba(0, 0, 0, 0.20);
}

.dropdown-submenu .right-submenu.show {
  display: block;
}

/* Icon tweaks */
.submenu-toggle {
  display: flex !important;
  justify-content: space-between;
  align-items: center;
}

.submenu-icon {
  transform: rotate(-90deg);
}

/* RESPONSIVENESS */
@media screen and (max-width: 1130px) {
  .custom-navbar {
    padding: 0 40px !important;
  }
}

/* DESKTOP: SHOW ON HOVER */
@media (min-width: 1024px) {
  .navbar-nav .dropdown .rotate-icon {
    transition: all 0.35s ease-in-out;
    rotate: 180deg;
  }

  .dropdown-label {
    cursor: default;
  }
}

/* MOBILE NAVIGATION */
@media (max-width: 991.98px) {
  nav {
    position: relative;
  }

/* Mobile menu (collapsed state) */
.navbar-menu {
  width: 50%;
  height: 100vh;
  padding: 20px !important;
  padding-left: 30px;
  position: fixed;
  top: 0;
  right: 0;
  background-color: #fff;
  box-shadow: 0 10px 20px -5px rgba(0, 0, 0, 0.2);
  margin: 0 !important;
  overflow: scroll;
  transition: transform 0.35s ease-in-out;
  transform: translateX(100%); /* hidden offscreen by default */
  z-index: 1050;
}

/* When open, slide back into view */
.navbar-menu.open {
  transform: translateX(0);
}


  .navbar-nav .nav-item {
    margin: 20px 0;
    display: block !important;
  }

  .navbar-nav .dropdown .custom-dropdown {
    display: block;
    position: static !important;
    margin-top: 10px !important;
    padding-left: 10px;
    background: lightcyan;
    box-shadow: none;
  }

  .dropdown-label {
    font-weight: 600;
    display: flex;
    gap: 5px;
  }

  .menu-toggle,
  .menu-toggle:active,
  .menu-toggle img {
    z-index: 4000;
    border: none !important;
    outline: none !important;
    background: none !important;
  }

  .menu-toggle:focus,
  .menu-toggle:focus-visible,
  .menu-toggle img:focus {
    outline: none !important;
    box-shadow: none !important;
  }

  /* Dropdown items take full width */
  .custom-dropdown .dropdown-item {
    padding: 20px 10px;
    width: 100%;
  }

  /* Submenus expand inline */
  .dropdown-submenu .right-submenu {
    position: static !important;
    display: none;
    margin-left: 15px;
    box-shadow: none;
    padding-left: 10px;
  }

  .dropdown-submenu .right-submenu.show {
    display: block;
    margin-top: 5px;
    background-color: cyan;
  }

  .dropdown-submenu .right-submenu.show .dropdown-item {
    padding-top: 20px;
    padding-bottom: 20px;
  }

  .submenu-toggle {
    display: flex;
    justify-content: space-between;
    width: 100%;
  }
}

@media screen and (max-width: 768px) {
  .navbar-menu {
    width: 70%;
  }
}

@media screen and (max-width: 550px) {
  .custom-navbar {
    padding: 0 20px !important;
  }

  .navbar-menu {
    width: 100%;
  }

  .navbar-nav .nav-item {
    margin: 20px auto;
  }

  .gradient-button-container {
    margin: 0 auto;
  }

  .dropdown-link {
    justify-content: center;
  }
}

/* NAVBAR ICONS */
.nav-icon {
  width: 24px;
  height: 24px;
  object-fit: contain;
  cursor: pointer;
}
